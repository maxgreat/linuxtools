Different tools to configure programmation env

Python Startup:
	to enable line completion, history, etc, on python

Nanorc:
	enable color in nano for different languages
	use : python editnano.py >> ~/.nanorc
